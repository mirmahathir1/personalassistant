# Listen/speak deps for the assistant: listening via local push-to-talk
# transcription (faster-whisper) and speaking via Piper text-to-speech, both
# required. sounddevice/numpy are shared. The base OpenAI client is installed
# via api/requirements.txt.
faster-whisper
piper-tts
sounddevice
numpy
tiktoken
# Bengali support (--lang bn), all local at runtime: Argos Translate for
# bn<->en translation, and Facebook MMS-TTS (via transformers/torch) to speak
# Bengali since Piper has no Bengali voice.
argostranslate
transformers
torch
