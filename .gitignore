# --- Secrets ---
api_key.txt
.env
.env.*

# --- Persisted runtime data ---
backend/chat_history.json
backend/chat_history.json.tmp
# Retrieval embedding cache (rebuilt from chat_history.json on demand).
backend/chat_vectors.npy
backend/chat_vectors.keys.json
# Long-term extracted-fact memory store.
backend/facts.json
backend/facts.json.tmp
backend/settings.json
backend/settings.json.tmp
# Per-character threads: index + each character's history/facts/vectors live
# under backend/data/<char_id>/. User data — kept out of git.
backend/data/

# --- Python ---
backend/.venv/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
frontend/node_modules/
frontend/dist/
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
*.swp
*~

# --- Local models: Kokoro TTS + Llama/embedding/Whisper (all under models/) ---
# Kokoro neural TTS model + voices (~340MB) and the Llama/embedding GGUF blobs
# live under models/ and are pulled on first run by run.sh. The download steps
# are documented there. Kept out of git so the repo stays small.
models/
