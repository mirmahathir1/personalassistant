fastapi==0.115.6
uvicorn[standard]==0.34.0
# OpenAI-compatible client — talks to the local Ollama server (no cloud calls).
openai==1.59.6
python-multipart==0.0.20
# Local neural TTS (offline, ONNX on CPU) — noticeably more natural than Piper.
# Ships its own espeak-ng via espeakng-loader, so no system package is needed.
kokoro-onnx==0.5.0
# Local speech-to-text (offline Whisper, on CPU). `requests` is an undeclared
# runtime dep of faster-whisper's model downloader, so pin it explicitly.
faster-whisper==1.1.0
requests==2.32.3
# Hybrid retrieval over the conversation archive (keeps prompts bounded
# without ever discarding history): BM25 lexical search + embedding vectors.
rank-bm25==0.2.2
numpy>=1.26
# Restores the stdlib `audioop` module removed in Python 3.13 (WAV splicing).
audioop-lts==0.2.2; python_version >= "3.13"
